# 환경변수 (절대 커밋 금지)
.env
.env.local
.env.*.local

# SSH keys (local development server access)
/key/

# Local Codex conversation export
/frontend/my-chat-session.json

# macOS
.DS_Store

# IDE
.idea/
*.iml
.vscode/

# Docker
docker-compose.override.yml

# Frontend
frontend/node_modules/
frontend/dist/

# Backend
backend/build/
backend/.gradle/
docs/

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
