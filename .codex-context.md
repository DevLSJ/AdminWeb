# DGuard KMS 어드민 웹 — 개발 컨텍스트

> 이 파일은 AI 코드 어시스턴트 및 개발자가 프로젝트 전체 맥락을 빠르게 파악하기 위한 참조 문서입니다.

---

## 프로젝트 개요

- **과제명**: 아이넵 통합키관리(D'GuardKMS) 어드민 웹 개발
- **GitHub**: https://github.com/devlsj/AdminWeb
- **기간**: 08/17(월) ~ 09/11(금) — 구현 4주
- **최종 발표**: 09/14(월)
- **개발 서버**: 192.168.200.53 (root: 설치·설정 / dguard: 빌드·기동)

---

## 기술 스택

### 백엔드
| 항목 | 기술 | 버전 |
|---|---|---|
| Language | Java | 21 (OpenJDK, Homebrew) |
| Framework | Spring Boot | 4.1.0 |
| Build | Gradle Wrapper | 9.5.1 |
| ORM | Spring Data JPA | Boot 내장 |
| Security | Spring Security + JWT | jwt 0.12.6 |
| API 문서 | SpringDoc OpenAPI | 2.8.9 (Swagger UI) |
| DB Driver | PostgreSQL JDBC | Boot 내장 |
| 기타 | Lombok, DevTools, Bean Validation | — |

### 프론트엔드
| 항목 | 기술 | 버전 |
|---|---|---|
| Language | TypeScript | 6.0 |
| Framework | React | 19 |
| Bundler | Vite | 8.2 |
| UI Library | MUI (Material UI) | v9.3 |
| 라우팅 | React Router DOM | 7.18 |
| HTTP 클라이언트 | axios | 1.19 |
| 런타임 | Node.js | 22 |
| Lint | oxlint | — |

### 인프라
| 항목 | 기술 | 버전/비고 |
|---|---|---|
| DB | PostgreSQL | 17 (Docker 컨테이너) |
| 웹서버 | Nginx | 1.27 (Docker) |
| 컨테이너 | Docker | 29.7.2 |
| 오케스트레이션 | Docker Compose | — |
| CI/CD | GitHub Actions | deploy.yml |
| 이미지 저장소 | Docker Hub | devlsj 계정 |

### 암호화
| 항목 | 알고리즘 |
|---|---|
| 마스터키 유도 | PBKDF2-HMAC-SHA256 (iterations: 환경변수, 기본/최소 10,000) |
| 데이터 암호화 | AES-256-GCM |
| 비밀번호 해시 | PBKDF2-HMAC-SHA256 + Salt |
| 무결성 검증 | HMAC-SHA256 |
| 바이너리 저장 | DB `bytea`, API 전송 시 Base64 |

---

## 프로젝트 구조

```
my-project/
├── .github/workflows/deploy.yml   ← CI/CD (수정 금지)
├── .gitignore                     ← (수정 금지)
├── .env.example                   ← 환경변수 템플릿 (수정 금지)
├── .codex-context.md              ← 이 파일
├── docker-compose.yml             ← 컨테이너 통합 (수정 금지)
├── docs/                          ← 과제 안내서
│
├── backend/                       ← Spring Boot
│   ├── Dockerfile                 ← (수정 금지)
│   ├── gradlew                    ← (수정 금지)
│   ├── gradle/wrapper/            ← (수정 금지)
│   ├── build.gradle               ← 라이브러리 추가 시만 수정
│   ├── settings.gradle
│   └── src/
│       ├── main/
│       │   ├── java/com/ineb/dguard_kms/
│       │   │   ├── config/        ← SecurityConfig, CryptoConfig
│       │   │   ├── common/        ← ApiResponse, PageResponse, GlobalExceptionHandler
│       │   │   ├── security/      ← JwtTokenProvider, JwtAuthenticationFilter, AdminUserDetails
│       │   │   ├── crypto/        ← CryptoUtil, MasterKeyService, IntegrityService
│       │   │   └── domain/
│       │   │       ├── auth/      ← controller, service, dto
│       │   │       ├── user/      ← controller, service, entity
│       │   │       ├── key/       ← controller, service, repository, entity
│       │   │       ├── notice/    ← controller, service, entity
│       │   │       ├── audit/     ← controller, service, entity
│       │   │       └── dashboard/ ← controller
│       │   └── resources/
│       │       └── application.properties  ← 설정값 추가 시만 수정
│       └── test/
│
└── frontend/                      ← React + MUI
    ├── Dockerfile                 ← (수정 금지)
    ├── nginx.conf                 ← (수정 금지)
    ├── package.json               ← npm 패키지 추가 시만 수정
    ├── vite.config.ts
    └── src/                       ← ✅ 개발 작업 영역
        ├── main.tsx
        ├── App.tsx
        └── (앞으로 만들 컴포넌트)
```

---

## 코드 작업 규칙

### ✅ 작업 가능한 영역
- `backend/src/` — 모든 Java 소스 코드
- `frontend/src/` — 모든 React/TypeScript 소스 코드

### ⚠️ 예외적으로 수정 가능 (해당 목적에 한해서만)
| 파일 | 수정 가능 조건 |
|---|---|
| `backend/build.gradle` | 새 라이브러리(의존성) 추가 시 |
| `backend/src/main/resources/application.properties` | 새 설정값 추가 시 |
| `frontend/package.json` | 새 npm 패키지 추가 시 |

### ❌ 절대 수정 금지
| 파일 | 이유 |
|---|---|
| `.github/workflows/deploy.yml` | CI/CD 파이프라인 |
| `docker-compose.yml` | 서버 컨테이너 구성 |
| `backend/Dockerfile` | 백엔드 이미지 빌드 설정 |
| `frontend/Dockerfile` | 프론트엔드 이미지 빌드 설정 |
| `frontend/nginx.conf` | Nginx 서빙 + API 프록시 설정 |
| `backend/gradlew` | Gradle Wrapper 실행 스크립트 |
| `backend/gradle/wrapper/` | Gradle Wrapper 바이너리 |
| `.gitignore` | Git 제외 파일 목록 |
| `.env.example` | 환경변수 템플릿 |

---

## 환경변수

### application.properties 연동 변수
| 환경변수 | 용도 | 로컬 기본값 |
|---|---|---|
| `SPRING_DATASOURCE_URL` | DB 연결 URL | `localhost:5432/dguard_kms` |
| `SPRING_DATASOURCE_USERNAME` | DB 계정 | `dguard` |
| `SPRING_DATASOURCE_PASSWORD` | DB 비밀번호 | 필수 입력 |
| `SPRING_JPA_HIBERNATE_DDL_AUTO` | DDL 전략 | `create` (운영: `validate`) |
| `JWT_SECRET` | JWT 서명 키 | 필수 입력 |
| `KMS_MASTER_PASSPHRASE` | 마스터키 유도 패스프레이즈 (20자↑) | 필수 입력 |
| `INTEGRITY_HMAC_KEY` | 무결성 HMAC 키 | 필수 입력 |
| `KMS_PBKDF2_ITERATIONS` | 마스터키 PBKDF2 반복 횟수 | `10000` |

### GitHub Secrets (CI/CD)
`SERVER_HOST`, `SERVER_USER`, `SERVER_SSH_KEY`, `DOCKER_USERNAME`, `DOCKER_PASSWORD`,
`DB_PASSWORD`, `KMS_MASTER_PASSPHRASE`, `INTEGRITY_HMAC_KEY`, `JWT_SECRET`

---

## 배포 흐름

```
git push → main
    ↓
GitHub Actions
    ├── build-backend  (병렬) → Docker Hub: devlsj/dguard-backend
    └── build-frontend (병렬) → Docker Hub: devlsj/dguard-frontend
    ↓ 완료 후
deploy
    ├── docker-compose.yml → SCP → 서버 /home/dguard/app/
    └── SSH → docker compose pull + up -d
    ↓
서버 192.168.200.53
    ├── dguard-db       (PostgreSQL 17, 데이터: /home/dguard/app/pgdata/)
    ├── dguard-backend  (Spring Boot :8080)
    └── dguard-frontend (Nginx :80, /api/* → backend 프록시)
```

---

## DB 테이블 목록

| 테이블 | 설명 | 주차 |
|---|---|---|
| `crypto_config` | 마스터키 유도 설정 (salt, kcv, iterations) | 1주 |
| `admin_user` | 관리자 계정 | 1주 |
| `crypto_key` | KMS 관리 키 메타 | 2주 |
| `key_material` | 래핑된 키 값 (마스터키로 래핑) | 2주 |
| `key_status_history` | 키 상태 전이 이력 | 2주 |
| `key_usage_log` | 키 사용 로그 | 2주 |
| `app_user` | 서비스 사용자 | 3주 |
| `notice` | 공지사항 | 4주 |
| `notice_file` | 첨부파일 (마스터키 암호화) | 4주 |
| `audit_log` | 감사 로그 (append-only, 해시 체인) | 3주 |

---

## 키 생명주기 상태 전이 (OASIS KMIP 표준 반영)

### KMIP 표준 키 상태 6종

| 상태 (영문) | 한글 | 설명 |
|---|---|---|
| Pre-Active | 생성됨 | 키가 생성됐으나 아직 사용 불가 |
| Active | 활성화 | 정상 사용 가능 상태 |
| Deactivated | 비활성화 | 사용 중단, 복호화·검증 등 제한적 연산만 허용 |
| Compromised | 침해됨 | 키가 노출/유출된 것으로 판단된 상태 |
| Destroyed | 폐기됨 | 키 원문 삭제 완료 |
| Destroyed Compromised | 침해 후 폐기됨 | 침해된 키가 폐기됨 |

### KMIP 표준 허용 전이 전체

| # | From | To | 조건 / 비고 |
|---|---|---|---|
| 1 | 생성됨 (Pre-Active) | 활성화 (Active) | 일반적인 키 활성화 |
| 2 | 생성됨 (Pre-Active) | 침해됨 (Compromised) | 활성화 전 침해 발견 시 |
| 3 | 생성됨 (Pre-Active) | 폐기됨 (Destroyed) | 사용 없이 폐기 |
| 4 | 활성화 (Active) | 비활성화 (Deactivated) | 유효기간 만료 또는 명시적 비활성화 |
| 5 | 활성화 (Active) | 침해됨 (Compromised) | 활성 중 침해 발견 |
| 6 | 비활성화 (Deactivated) | 침해됨 (Compromised) | 비활성 상태에서 침해 발견 |
| 7 | 비활성화 (Deactivated) | 폐기됨 (Destroyed) | 비활성 키 폐기 |
| 8 | 침해됨 (Compromised) | 침해 후 폐기됨 (Destroyed Compromised) | 침해 키 폐기 처리 |
| 9 | 생성됨→침해됨 경로 | 침해 후 폐기됨 (Destroyed Compromised) | Pre-Active 상태에서 침해 후 폐기 |
| 10 | 폐기됨 (Destroyed) | *(없음)* | 최종 상태 — 모든 전이 금지 |
| 11 | 침해 후 폐기됨 (Destroyed Compromised) | *(없음)* | 최종 상태 — 모든 전이 금지 |

### 최신 상용 KMS(NCloud KMS) 참조 — 실제 서비스 상태 모델

> NCloud KMS(2026 기준) 는 KMIP 를 단순화하여 운영 편의성을 높인 4단계 모델을 사용한다.
> 이 과제 설계 시 참고하여 EXPIRED·DISTRIBUTED 확장 상태를 도입하였다.

| NCloud KMS 상태 | 한글 | 암/복호화 가능 | 관리 과금 | 비고 |
|---|---|:---:|:---:|---|
| 사용 가능 | Active | ✅ | ✅ | 생성 직후 자동 전환 |
| 사용 중지 | Deactivated | ❌ | ✅ | 재활성화 가능, 회전 주기 계속 적용 |
| 삭제 요청 | Pending Deletion | ❌ | ✅ | 요청 후 72시간 대기, 취소 시 사용 중지로 복원 |
| 삭제 | Destroyed | ❌ | ❌ | 원시 키(Raw key) 즉시 삭제, 복구 불가 |

**NCloud KMS 상태 전이:**
```
생성 → 사용 가능 ⇄ 사용 중지 → 삭제 요청 → 삭제
                                 (72시간 대기 중 취소 → 사용 중지로 복원)
```

### 키 버전 관리 (Key Versioning / Rotation)

> 상용 KMS 표준 관행. 키 자체를 교체하지 않고 버전을 증가시켜 연속성을 보장한다.

| 항목 | 내용 |
|---|---|
| 버전 생성 | 키 갱신(Rotate) 시 신규 버전 생성, 기존 버전은 유지 |
| 최대 버전 수 | 상용(NCloud 기준) 키당 최대 100개 버전 |
| 상태 상속 | 키 상태 변경 시 **모든 버전에 상속** (키 사용 중지 → 전 버전 사용 불가) |
| 상태 복원 | 키 재활성화 시 각 버전은 **이전 상태로 자동 복원** |
| 암호화 연속성 | 구 버전으로 암호화된 데이터는 구 버전으로 복호화 가능 |
| 신규 암호화 | 항상 **최신 버전(Primary Version)** 으로 암호화 |
| 이 프로젝트 구현 | `crypto_key.version` 컬럼으로 버전 추적, rotate 시 version +1 |

**키 갱신(Rotate) 흐름:**
```
1. 신규 키 원문 생성 (SecureRandom)
2. 마스터키로 AES-256-GCM 래핑 → key_material 신규 행 저장
3. crypto_key.version +1, status = CREATED
4. CREATED → ACTIVE 전이
5. 기존 버전 → INACTIVE 처리 (key_status_history 기록)
6. key_usage_log 에 ROTATE 기록
```

### 키 배포(Distribution) 과정

> KMS 관리 키가 외부 시스템에 전달되는 흐름. 이 과제에서 DISTRIBUTED 상태의 의미.

| 단계 | 설명 |
|---|---|
| 1. 키 요청 | 외부 시스템이 API로 특정 키(key_uid) 사용 요청 |
| 2. 상태 확인 | ACTIVE 상태인지 검증 (ACTIVE 아니면 거부) |
| 3. 무결성 검증 | integrity_hash 재계산 → 불일치 시 409 반환 |
| 4. 언래핑 | key_material.wrapped_key → 마스터키로 AES-256-GCM 복호화 |
| 5. 전달 | 평문 키를 TLS 채널을 통해 외부 시스템에 전달 |
| 6. 메모리 제로화 | Arrays.fill(keyBytes, (byte)0) — 전달 후 즉시 |
| 7. 상태 전이 | ACTIVE → DISTRIBUTED (외부 배포 사실 기록) |
| 8. 이력 기록 | key_status_history + key_usage_log 저장 |

> **중요**: 배포된 키의 원본 관리 책임은 이후 수신 시스템에 있다.
> 이 과제 어드민은 배포 사실(DISTRIBUTED 상태)만 기록하며, 수신 후 키 관리는 범위 외.

### 이 프로젝트 구현 상태와 KMIP 표준 비교

| KMIP 표준 | NCloud KMS | 이 프로젝트 구현 | 비고 |
|---|---|---|---|
| Pre-Active | *(생성 즉시 Active 전환)* | CREATED | 명시적 활성화 단계 유지 |
| Active | 사용 가능 | ACTIVE | 동일 |
| Deactivated | 사용 중지 | INACTIVE | 동일 개념 |
| Compromised | *(없음)* | COMPROMISED | KMIP 표준 반영 |
| Destroyed | 삭제 | DESTROYED | 동일 |
| Destroyed Compromised | *(없음)* | *(미구현)* | COMPROMISED → DESTROYED 로 통합 |
| *(없음)* | *(삭제 요청 72h 대기)* | *(미구현)* | 과제 범위 외 단순화 |
| *(없음)* | *(없음)* | EXPIRED | 과제 확장 — 만료 기간 관리 |
| *(없음)* | *(없음)* | DISTRIBUTED | 과제 확장 — 외부 배포 추적 |

> 발표 시: "OASIS KMIP 표준과 NCloud KMS 실제 운영 모델을 참고하여 설계했으며,
> 운영 편의를 위해 EXPIRED·DISTRIBUTED 상태를 확장하고,
> Destroyed Compromised 와 삭제 요청 대기(72h) 는 단순화했습니다."

### 이 프로젝트 구현 허용 전이 규칙

```
CREATED     → ACTIVE
ACTIVE      → EXPIRED | INACTIVE | DISTRIBUTED | COMPROMISED
EXPIRED     → INACTIVE | ACTIVE (재활성, reason 필수)
INACTIVE    → DESTROYED
DISTRIBUTED → DESTROYED
COMPROMISED → DESTROYED

암복호화 테스트: ACTIVE 상태에서만 허용
금지: DESTROYED에서 나가는 모든 전이
금지: CREATED → DESTROYED 직행
```

---

## 1주차 구현 목표 (08/17 ~ 08/21)

### 목업 우선순위 (화면 구현 순서)

> 목업을 먼저 만들어 화면 구조를 확정한 뒤 백엔드 API를 개발한다.
> MUI 컴포넌트로 바로 작성하여 목업이 곧 실제 코드가 되도록 한다.
> API 연동 전까지는 더미 데이터(하드코딩)로 화면을 구성한다.

**Step 1 — 기본 레이아웃** `frontend/src/layouts/`
- `AppBar` : 상단 헤더 (관리자명 `admin`, 로그아웃 버튼)
- `Drawer` : 좌측 사이드바 메뉴
  - 대시보드
  - 키 관리 (키 목록 / 키 등록 / 암복호화 테스트)
  - 사용자 관리
  - 감사 로그
  - 게시판
- `Box` : 우측 컨텐츠 영역
- 로그인 여부에 따라 레이아웃 표시/숨김 처리

**Step 2 — 로그인 화면** `frontend/src/pages/Login.tsx`
- 아이디 / 비밀번호 입력 (`TextField`)
- 로그인 버튼 (`Button`)
- 실패 시 오류 메시지 (`Alert`) — "아이디 또는 비밀번호가 올바르지 않습니다"
- 초기 계정: `admin / admin`
- 성공 시 JWT → `localStorage` 저장 → 레이아웃으로 이동

**Step 3 — 각 메뉴 화면 껍데기** `frontend/src/pages/`
더미 데이터로 화면 구조만 먼저 구성한다.

| 파일 | 화면 | 더미 데이터 |
|---|---|---|
| `Dashboard.tsx` | 대시보드 | 요약 카드 4개, 차트 영역 |
| `KeyList.tsx` | 키 목록 | 테이블 + 검색 필터 |
| `KeyDetail.tsx` | 키 상세 | 메타정보 + 상태 이력 타임라인 |
| `KeyRegister.tsx` | 키 등록 | 폼 (알고리즘, 키길이, 용도, 유효기간) |
| `CryptoTest.tsx` | 암복호화 테스트 | 평문 입력 → 암호문 출력 |
| `UserList.tsx` | 사용자 목록 | 테이블 + 마스킹 표시 |
| `AuditLog.tsx` | 감사 로그 | 테이블 + 기간 검색 |
| `NoticeList.tsx` | 공지사항 목록 | 테이블 |

**Step 4 — 라우팅 연결** `frontend/src/App.tsx`
```
/login          → Login.tsx       (인증 불필요)
/               → Dashboard.tsx   (인증 필요)
/keys           → KeyList.tsx     (인증 필요)
/keys/:keyUid       → KeyDetail.tsx   (인증 필요, :keyUid = key_uid UUID)
/keys/register  → KeyRegister.tsx (인증 필요)
/keys/test      → CryptoTest.tsx  (인증 필요)
/users          → UserList.tsx    (인증 필요)
/audit-logs     → AuditLog.tsx    (인증 필요)
/notices        → NoticeList.tsx  (인증 필요)
```

**Step 5 — axios 인스턴스** `frontend/src/api/axios.ts`
- 모든 요청에 `Authorization: Bearer {token}` 헤더 자동 주입
- 401 응답 시 localStorage 토큰 제거 → `/login` 자동 이동

**Step 6 — API 연동** (목업 완성 후)
- 백엔드 로그인 API 완성 시점에 더미 데이터 → 실제 API 교체

---

### 백엔드 우선순위
1. `CryptoConfig` — PBKDF2 마스터키 유도 + KCV 검증 (기동 실패 포함)
2. `SecurityConfig` — JWT 필터 체인 구성
3. `JwtTokenProvider` — 토큰 발급/검증
4. `JwtAuthenticationFilter` — 요청마다 토큰 검증
5. `AuthController / AuthService` — 로그인 API (`POST /api/auth/login`)
6. `AdminUser` 초기 데이터 — admin/admin (PBKDF2 해시+Salt 저장)

### 프론트엔드 우선순위
1. React Router 라우팅 구성 (`/login`, `/`)
2. axios 인스턴스 — JWT 헤더 자동 주입, 401 자동 로그아웃
3. 로그인 화면 (MUI `TextField`, `Button`, `Alert`)
4. 기본 레이아웃 (MUI `AppBar`, `Drawer`, `Box`)

### 1차 발표 통과 기준 (08/24)
- [ ] `admin/admin` 로그인 동작
- [ ] 패스프레이즈 오입력 시 KCV 검증 실패 → 기동 중단 시연
- [ ] `admin_user` 테이블에 비밀번호 평문 없음 (hash + salt만 존재)

---

## ⭐ 과제 핵심 개념 — 두 가지 키의 역할 차이

> 이 과제 전체를 관통하는 핵심. 발표마다 이 구분을 설명할 수 있어야 한다.

### 마스터키 (Master Key)

- **역할**: 어드민이 **자기 DB에 저장하는 데이터를 보호**하는 키
- **생성 방식**: 마스터 패스프레이즈 → PBKDF2-HMAC-SHA256(salt, 기본/최소 10,000회) → AES-256 키 32바이트 유도
- **보호 대상**:
  - 사용자 개인정보 (`app_user.name_ciphertext`, `phone_ciphertext`, `email_ciphertext`)
  - 게시판 첨부파일 (암호화된 바이너리)
  - KMS 관리 키 원문 래핑 (`key_material.wrapped_key`)
- **특징**:
  - 키 파일로 저장하지 않음 — 환경변수 패스프레이즈에서 매번 유도
  - `byte[]`로만 메모리 보관, 사용 후 `Arrays.fill(key, (byte)0)` 제로화 (`String` 사용 금지)
  - 기동 시 KCV(Key Check Value) 검증 → 불일치 시 **기동 중단 (fail-fast)**
  - 패스프레이즈 분실 = 모든 암호화 데이터 영구 복구 불가

### KMS 관리 키 (KMS Managed Key)

- **역할**: **외부 시스템이 연동해 사용하는** 데이터 암호화용 키
- **어드민의 역할**: 키 자체를 쓰는 게 아니라 키의 **생명주기 관리**와 **동작 검증**
  - 생명주기 관리: 등록 → 상태 전이 (CREATED → ACTIVE → EXPIRED → ...) → 이력 추적
  - 동작 검증: 암복호화 테스트 기능 (ACTIVE 상태에서만 허용)
- **저장 방식**: 키 원문을 마스터키로 AES-256-GCM **래핑**한 뒤 `key_material.wrapped_key`에 `bytea`로 저장
- **중요**: 어드민 자체 데이터 암호화에는 사용하지 않음

```
마스터 패스프레이즈 ──PBKDF2──▶ 마스터키 (AES-256)
                                    │
                    ┌───────────────┼────────────────────┐
                    ▼               ▼                    ▼
             사용자 개인정보   게시판 첨부파일      KMS 관리 키 래핑
             (직접 암호화)    (직접 암호화)         (key_material)
                                                         │
                                              외부 시스템 연동 / 암복호화 테스트
```

### 최종 목표 상태 (09/14 발표 기준)

| 목표 항목 | 내용 |
|---|---|
| 마스터키 유도 | 기동 시 패스프레이즈 → PBKDF2 유도 → KCV 검증 → 불일치 시 기동 중단 |
| KMS 관리 키 생명주기 | 등록·조회·상태 전이·이력 관리 + 암복호화 테스트 기능 |
| 사용자 비밀번호 | 단방향 PBKDF2-HMAC-SHA256 + Salt (원문 복원 불가) |
| 사용자 개인정보 | 연락처·이메일 마스터키 AES-256-GCM 암호화, 조회 시 마스킹 |
| 게시판 첨부파일 | 마스터키 AES-256-GCM 암호화 저장 → 복호화 다운로드 |
| 무결성 검증 | `crypto_key`, `app_user`, `audit_log` — HMAC-SHA256 체인으로 DB 직접 수정 탐지 |
| 감사 로그 | 모든 관리자 행위 자동 기록 (append-only, 해시 체인) |
| HSM 대비 설명 | 실제 D'GuardKMS는 물리 HSM + 스마트카드 2단계 인증. 이 과제는 환경변수 패스프레이즈 유도로 축소한 것 — 무엇을 포기했는지 설명 가능해야 함 |

---

## ⭐ 2주차 핵심 목표 (08/24 ~ 08/28) — KMS 관리 키 · 암복호화 테스트 · 무결성

> **이번 주가 과제의 본체다.**
> 마스터키로 키를 래핑·언래핑하고, 상태 전이 규칙을 코드로 강제하고,
> DB 직접 수정을 무결성 해시로 탐지하는 세 가지를 완성한다.

### 핵심 구현 3가지

#### 1. 키 래핑 / 언래핑 (key_material)
- `SecureRandom`으로 키 원문(keyBytes) 생성 → `AES-256-GCM`으로 **마스터키 래핑** → `bytea` 저장
- 암복호화 테스트 시: `wrapped_key` 조회 → 마스터키로 **언래핑** → 사용 후 `Arrays.fill(keyBytes, (byte)0)`
- **DB에 키 평문이 존재하면 안 됨** (2차 발표 체크 항목)

#### 2. 상태 전이 규칙 강제 (key_status_history)
```
CREATED   → ACTIVE
ACTIVE    → EXPIRED | INACTIVE | DISTRIBUTED | COMPROMISED
EXPIRED   → INACTIVE | ACTIVE (재활성, reason 필수)
INACTIVE  → DESTROYED
DISTRIBUTED → DESTROYED
COMPROMISED → DESTROYED
DESTROYED → 모든 전이 금지
CREATED   → DESTROYED 직행 금지
암복호화 테스트 → ACTIVE 상태에서만 허용
```
- 허용되지 않는 전이 요청 → `400 Bad Request` 또는 `409 Conflict` 반환
- `key_status_history`에 전이 기록 저장 (reason 필수, append-only)

#### 3. 무결성 해시 (crypto_key.integrity_hash)
- 키 생성·수정 시: 지정된 필드를 정규화 후 `HMAC-SHA256(INTEGRITY_HMAC_KEY, 정규화 문자열)` 계산 → Base64 저장
- 키 조회 시: 저장된 `integrity_hash` 재계산 후 대조 → 불일치 시 `409 Conflict` + `integrityValid: false` 응답
- **DB 직접 수정 시 위반 탐지** 시연 필수 (2차 발표)

### 2주차 백엔드 구현 순서

1. **`CryptoKey` / `KeyMaterial` Entity · Repository** — 테이블 매핑
2. **`MasterKeyService`** — 마스터키 언래핑 유틸 (기존 1주차 CryptoConfig 연장)
3. **`KeyService`** — 키 생성 (SecureRandom → 래핑 → 저장) + 무결성 해시 생성
4. **`KeyStatusService`** — 상태 전이 규칙 검증 + `key_status_history` 저장
5. **`IntegrityService`** — HMAC-SHA256 계산 · 검증 공통 유틸
6. **`KeyController`** — REST API 9개 (목록·생성·상세·수정·상태전이·이력·사용통계·암호화테스트·복호화테스트)
7. **`KeyUsageLog`** — 암복호화 테스트 호출 시 자동 기록

### 2주차 프론트엔드 구현 순서

1. **`KeyList.tsx`** — 키 목록 테이블 + 키워드·알고리즘·상태·용도 필터 + 무결성 상태 뱃지
2. **`KeyRegister.tsx`** — 키 등록 폼 (알고리즘·키길이·용도·유효기간)
3. **`KeyDetail.tsx`** — 메타정보 + 상태 전이 버튼 (허용 전이만 노출) + 이력 타임라인
4. **`CryptoTest.tsx`** — 키 선택 → 평문 입력 → 암호화 → 암호문+IV 입력 → 복호화 결과 확인

### 2주차 화면별 라우팅

| 경로 | 컴포넌트 | 설명 |
|---|---|---|
| `/keys` | `KeyList.tsx` | 키 목록 (검색·필터·페이징) |
| `/keys/register` | `KeyRegister.tsx` | 키 등록 |
| `/keys/:keyUid` | `KeyDetail.tsx` | 키 상세 + 상태 전이 + 이력 (`:keyUid` = key_uid UUID) |
| `/keys/test` | `CryptoTest.tsx` | 암복호화 테스트 |

### 2차 발표 통과 기준 (08/31)

- [ ] 키 CRUD 및 상태 전이 차단 동작 확인
- [ ] 허용되지 않는 상태 전이 요청 시 400/409 반환 시연
- [ ] 암복호화 테스트 기능 — ACTIVE 키 선택 → 평문 암호화 → 복호화 → 원문 복원 확인
- [ ] `key_material.wrapped_key` 컬럼에 키 평문 없음 (bytea 바이너리만 존재)
- [ ] DB 직접 수정 후 `integrity_hash` 불일치 → 무결성 위반 탐지 시연

---

## REST API 목록 (전체 32개 + 심화 1개)

> 경로·구성은 예시이며 변경 가능. 변경 시 API 명세서에 반영 필요.

### 1주차 — 인증

| 메서드 | 경로 | 설명 | 주요 처리 |
|---|---|---|---|
| POST | `/api/auth/login` | 관리자 로그인 · JWT 발급 | PBKDF2 해시 검증 → JWT 발급 |
| GET | `/api/auth/me` | 내 정보 조회 | JWT 토큰에서 사용자 정보 반환 |
| POST | `/api/auth/logout` | 로그아웃 | 클라이언트 토큰 파기 |

**요청/응답 예시:**
```
POST /api/auth/login
Request:  { "loginId": "admin", "password": "admin" }
Response: { "success": true, "data": { "token": "eyJ...", "name": "관리자", "role": "ADMIN" } }

GET /api/auth/me
Header: Authorization: Bearer {token}
Response: { "success": true, "data": { "loginId": "admin", "name": "관리자", "role": "ADMIN" } }
```

---

### 2주차 — KMS 관리 키

| 메서드 | 경로 | 설명 | 주요 처리 |
|---|---|---|---|
> **경로 파라미터는 DB 순번(`id`) 노출 금지 규칙에 따라 모두 `{key_uid}` (UUID v4) 를 사용한다.**

| GET | `/api/keys` | 키 목록 | 쿼리: keyword, algorithm, status, purpose, page, size, sort |
| POST | `/api/keys` | 키 생성 | SecureRandom 키 생성 → 마스터키로 AES-256-GCM 래핑 저장 → HMAC 무결성 해시 생성 |
| GET | `/api/keys/{key_uid}` | 키 상세 | 키 값(wrapped) 미반환, 무결성 검증 결과 포함 |
| PUT | `/api/keys/{key_uid}` | 키 메타정보 수정 | key_name, purpose, expire_at 수정 → 무결성 해시 재계산 |
| PATCH | `/api/keys/{key_uid}/status` | 상태 전이 | 사유(reason) 필수, 허용 전이 규칙 위반 시 400/409 반환 |
| GET | `/api/keys/{key_uid}/history` | 상태 전이 이력 | key_status_history 전체 조회 |
| GET | `/api/keys/{key_uid}/usage` | 키 사용 통계 | key_usage_log 기준 총/성공/실패 횟수 |
| POST | `/api/keys/{key_uid}/test/encrypt` | 암호화 테스트 | ACTIVE 키만 허용 → 마스터키로 언래핑 → 평문 AES-256-GCM 암호화 → Base64 반환, key_usage_log 기록 |
| POST | `/api/keys/{key_uid}/test/decrypt` | 복호화 테스트 | ACTIVE 키만 허용 → 마스터키로 언래핑 → 암호문+IV 복호화 → 평문 반환, key_usage_log 기록 |

**상태 전이 허용 규칙:**
```
CREATED   → ACTIVE
ACTIVE    → EXPIRED / INACTIVE / DISTRIBUTED / COMPROMISED
EXPIRED   → INACTIVE / ACTIVE (재활성, 사유 필수)
INACTIVE  → DESTROYED
DISTRIBUTED → DESTROYED
COMPROMISED → DESTROYED

금지: DESTROYED에서 나가는 모든 전이
금지: CREATED → DESTROYED 직행
암복호화 테스트: ACTIVE 상태에서만 허용
```

**요청/응답 예시:**
```
POST /api/keys
Request: { "keyName": "KEY-001", "algorithm": "AES", "keySize": 256, "purpose": "ENCRYPT", "expireAt": "2026-12-31" }
Response: { "success": true, "data": { "keyUid": "uuid", "keyName": "KEY-001", "status": "CREATED", "integrityValid": true } }

PATCH /api/keys/{key_uid}/status
Request: { "toStatus": "ACTIVE", "reason": "운영 환경 배포" }
Response: { "success": true, "data": { "fromStatus": "CREATED", "toStatus": "ACTIVE", "changedAt": "2026-08-19 09:00:00" } }

POST /api/keys/{key_uid}/test/encrypt
Request: { "plaintext": "Hello World" }
Response: { "success": true, "data": { "ciphertext": "Base64...", "iv": "Base64..." } }

POST /api/keys/{key_uid}/test/decrypt
Request: { "ciphertext": "Base64...", "iv": "Base64..." }
Response: { "success": true, "data": { "plaintext": "Hello World" } }
```

---

### 3주차 — 사용자 관리 · 감사 로그

| 메서드 | 경로 | 설명 | 주요 처리 |
|---|---|---|---|
| GET | `/api/users` | 사용자 목록 | 마스킹 응답(phone/email) · 연락처는 HMAC 해시 기반 정확검색만 지원 (LIKE 불가) |
| GET | `/api/users/{id}` | 사용자 상세 | 마스킹 응답 · 무결성 검증 결과 포함 |
| POST | `/api/users/{id}/plain` | 원문 조회 | ADMIN 권한 한정 · 조회 사유 필수 · 마스터키 복호화 · no-store · 감사로그 자동 기록 |
| POST | `/api/users` | 사용자 등록 | 비밀번호 PBKDF2 해시+Salt · 연락처/이메일 마스터키 AES-256-GCM 암호화 · 검색용 HMAC 해시 저장 · 무결성 해시 생성 |
| PUT | `/api/users/{id}` | 사용자 수정 | 연락처 재암호화 · 검색 해시 재계산 · 무결성 해시 재계산 |
| POST | `/api/users/{id}/password` | 비밀번호 재설정 | 새 Salt 생성 → PBKDF2 재해시 (원문 조회 불가) |
| PATCH | `/api/users/{id}/status` | 상태 변경 | ACTIVE ↔ INACTIVE |
| GET | `/api/audit-logs` | 감사로그 검색 | 쿼리: from, to, actor, action, page · append-only |
| GET | `/api/audit-logs/verify` | 해시 체인 검증 | prev_hash + row_hash 체인 검증 → 변조·삭제된 구간 반환 |
| GET | `/api/audit-logs/export` | CSV 내려받기 | Content-Disposition: attachment 스트리밍 다운로드 |

**마스킹 규칙:**
```
phone:  010-1234-5678 → 010-****-5678
email:  hong@example.com → ho***@example.com
```

**감사로그 자동 기록 대상 행위:**
```
LOGIN, LOGOUT, KEY_CREATE, KEY_STATUS_CHANGE, KEY_TEST,
USER_CREATE, USER_UPDATE, USER_VIEW_PLAIN, USER_STATUS_CHANGE, USER_PASSWORD_RESET, AUDIT_EXPORT,
NOTICE_CREATE, NOTICE_UPDATE, NOTICE_DELETE, FILE_DOWNLOAD
```

---

### 4주차 — 게시판 · 대시보드

| 메서드 | 경로 | 설명 | 주요 처리 |
|---|---|---|---|
| GET | `/api/notices` | 공지 목록 | 쿼리: title(검색), exposeYn, page |
| GET | `/api/notices/{id}` | 공지 상세 | 조회 시 view_count +1 자동 증가 |
| POST | `/api/notices` | 공지 등록 | multipart/form-data · 첨부파일 마스터키 AES-256-GCM 암호화 저장 · IV/enc_ver 저장 |
| PUT | `/api/notices/{id}` | 공지 수정 | 본문·노출여부 수정 |
| DELETE | `/api/notices/{id}` | 공지 삭제 | 첨부파일(암호화된 바이너리) 함께 정리 |
| GET | `/api/files/{id}/download` | 첨부 복호화 다운로드 | 마스터키로 복호화 → 원본 파일 스트리밍 · 감사로그 기록 |
| DELETE | `/api/files/{id}` | 첨부 삭제 | 파일 + notice_file 레코드 삭제 |
| GET | `/api/dashboard/summary` | 요약 통계 | 전체 키 수, 활성 키 수, 사용자 수, 공지 수, 만료 임박 키 수, 무결성 위반 건수 |
| GET | `/api/dashboard/expiring` | 만료 임박 키 목록 | 쿼리: days(기본 30) · ACTIVE 상태 키 중 만료 임박 순 정렬 |
| GET | `/api/dashboard/usage-trend` | 30일 키 사용 추이 | key_usage_log 기준 일자별 성공/실패 횟수 |

**대시보드 요약 응답 예시:**
```json
{
  "totalKeys": 128,
  "activeKeys": 42,
  "totalUsers": 15,
  "totalNotices": 23,
  "expiringKeys": 3,
  "integrityViolations": {
    "cryptoKey": 0,
    "appUser": 0,
    "auditLog": 0
  }
}
```

---

### 심화 — 키 교체

| 메서드 | 경로 | 설명 | 주요 처리 |
|---|---|---|---|
| POST | `/api/keys/{key_uid}/rotate` | KMS 관리 키 교체 | 신규 버전 키 생성 → 이전 버전 INACTIVE 처리 |

---

## 공통 규격 상세

### 응답 포맷
```json
{ "success": true/false, "data": {}, "message": "string", "errorCode": "string" }
```

### 페이징 응답
```json
{ "content": [], "page": 0, "size": 20, "totalElements": 100, "totalPages": 5 }
```

### HTTP 상태 코드
| 코드 | 의미 |
|---|---|
| 200 | 성공 |
| 400 | 입력값 오류 · 상태 전이 규칙 위반 |
| 401 | 미인증 (JWT 없음/만료) |
| 403 | 권한 없음 (ADMIN 전용 API 접근) |
| 404 | 리소스 없음 |
| 409 | 상태 충돌 · 무결성 위반 |
| 500 | 서버 오류 |

### 날짜 규격
- 저장: UTC (타임존 포함)
- 응답: `yyyy-MM-dd HH:mm:ss` KST

### 암호화 컬럼 검색 제약
- `phone_enc`, `email_enc` 등 암호화 컬럼은 평문 LIKE 검색 불가
- HMAC 해시 컬럼(`phone_hash`, `email_hash`)으로 **정확검색만** 지원
- 부분검색 미지원 — API 명세에 명시 필요

### 외부 식별자
- 순번(auto increment) 노출 금지
- `key_uid` (UUID v4) 사용

---

## 공통 응답 규격

```json
{
  "success": true,
  "data": { },
  "message": "string",
  "errorCode": "string"
}
```

페이징:
```json
{
  "content": [],
  "page": 0,
  "size": 20,
  "totalElements": 100,
  "totalPages": 5
}
```

HTTP 상태 코드: `200` / `400` / `401` / `403` / `404` / `409` / `500`
